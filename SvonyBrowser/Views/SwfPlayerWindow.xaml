<Window x:Class="SvonyBrowser.Views.SwfPlayerWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf" Title="SWF Player" Height="600" Width="800" WindowStartupLocation="CenterScreen" Background="#1E1E1E">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Toolbar -->
    <Border Grid.Row="0" Background="#2D2D2D" Padding="8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="FilePathText" Grid.Column="0" Text="No file loaded" Foreground="#CCCCCC" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
          <Button x:Name="OpenButton" Content="Open SWF" Click="OpenButton_Click" Padding="12,6" Margin="4,0" Background="#0078D4" Foreground="White" BorderThickness="0"/>
          <Button x:Name="ReloadButton" Content="Reload" Click="ReloadButton_Click" Padding="12,6" Margin="4,0" Background="#3D3D3D" Foreground="White" BorderThickness="0"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Browser -->
    <cef:ChromiumWebBrowser x:Name="Browser" Grid.Row="1" FrameLoadEnd="Browser_FrameLoadEnd"/>

    <!-- Status Bar -->
    <Border Grid.Row="2" Background="#007ACC" Padding="8,4">
      <TextBlock x:Name="StatusText" Text="Ready" Foreground="White" FontSize="11"/>
    </Border>
  </Grid>
</Window>
